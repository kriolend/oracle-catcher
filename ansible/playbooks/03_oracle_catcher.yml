---
- name: Deploy Oracle Catcher on Master node
  hosts: master
  become: true
  roles:
    - oracle_catcher
  # Why master only: Oracle Catcher is one process. It hunts for the ARM instance.
  # We do not run it on workers. This prevents API block (429 rate limit).
